# 🗂️ Инструкция: справочник «Должники» через Конфигуратор 1С:УТ 3.2 (базовая)

## 🧭 Что сделаем
Создадим новый объект «Справочник.Должники» в конфигураторе УТ 3.2, добавим реквизиты и формы, обновим базу и настроим права. Поле контрагента будет строковым.

## ✅ Предпосылки
- Базовая лицензия УТ 3.2, доступ к конфигуратору.
- Перед изменениями — сделайте резервную копию ИБ.

## ⚙️ Шаг 1. Открыть конфигурацию
1. Закройте пользовательский сеанс, откройте ИБ в Конфигураторе.
2. Меню «Конфигурация» → «Открыть конфигурацию».

## 🧩 Шаг 2. Создать справочник
1. В дереве: «Справочники» → правая кнопка → «Добавить».
2. Имя: `Должники`. Синоним: «Должники».
3. «Использование стандартных команд» — «Обычное».

## 📐 Шаг 3. Реквизиты
В свойствах справочника → «Реквизиты» → «Добавить»:
- `Контрагент` — Строка (например, длина 150).
- `СуммаДолга` — Число(15,2).
- `ДатаВзятияДолга` — Дата.
- `ДатаВозвратаДолга` — Дата.
- `Комментарий` — Строка(200) (по желанию).

## 🪟 Шаг 4. Формы
1. В объекте «Должники» → «Формы» → «Форма списка» → «Открыть» → «Автосоздание».
2. В таблицу списка добавьте колонки: Контрагент, СуммаДолга, ДатаВзятияДолга, ДатаВозвратаДолга, Комментарий.
3. «Форма элемента» → «Открыть» → «Автосоздание» → расставьте поля в удобном порядке.

## 🔗 Шаг 5. Команды и размещение
- В свойствах справочника включите стандартную команду «ОткрытьСписок» (чтобы пункт появился в интерфейсе).
- При необходимости добавьте команду «Открыть список» в разделе «НСИ и администрирование» через настройки команд разделов.
- Можно включить «Отображать в панели функций» для быстрого доступа.

## 🚀 Шаг 6. Обновить конфигурацию базы
1. «Конфигурация» → «Проверить» (убедиться, что нет ошибок).
2. «Конфигурация» → «Обновить конфигурацию базы данных» → согласиться на перезапуск.
3. Запустить базу в пользовательском режиме.

## ✍️ Шаг 7. Ввод данных
1. Откройте «Должники» (через раздел или «Все функции» → Справочники).
2. Нажмите «Создать» → заполните Контрагента, Сумму долга, Даты, Комментарий → «Записать и закрыть».

## 🔍 Работа со списком
- Фильтруйте по датам (взял/вернул), чтобы видеть просрочки.
- Сортируйте по сумме долга для приоритизации.
- Используйте поиск по строковому полю Контрагент.

## 🔑 Доступ для пользователя «Кассир»
1. В конфигураторе: «Роли» → откройте роль кассира (или создайте новую, например `Кассир_Должники`).
2. В дереве прав: «Справочники» → «Должники» → отметьте «Чтение», «Запись», «Использование» (удаление — по необходимости).
3. Обновите конфигурацию базы данных.
4. В пользовательском режиме: «НСИ и администрирование» → «Настройка пользователей и прав» → пользователь «Кассир» → добавьте роль с правами на «Должники» → «Записать».
5. Перезапустите сеанс кассира и проверьте доступ.

## ➕ Как посчитать сумму
### По выбранному сегменту (если поле есть)
1. Добавьте реквизит справочника `Сегмент` (строка или перечень).
2. В форме списка добавьте реквизит формы `ОтборСегмент` и поле ввода.
3. Создайте команду «ПосчитатьСумму» и кнопку.
4. Обработчик:
```1c
Процедура ПосчитатьСумму(Команда)
    Итог = 0;
    Для каждого Стр Из ЭлементыФормы.Список.ТекущиеДанные Цикл
        Если ПустаяСтрока(ОтборСегмент) Или Стр.Сегмент = ОтборСегмент Тогда
            Итог = Итог + Стр.СуммаДолга;
        КонецЕсли;
    КонецЦикла;
    Сообщить("Сумма по сегменту: " + Формат(Итог, "ЧГ=15.2"));
КонецПроцедуры
```

### Если поля `Сегмент` нет
- Можно суммировать строки, отфильтрованные стандартными отборами формы:
```1c
Процедура ПосчитатьСумму(Команда)
    Итог = 0;
    Для каждого Стр Из ЭлементыФормы.Список.ТекущиеДанные Цикл
        Итог = Итог + Стр.СуммаДолга;
    КонецЦикла;
    Сообщить("Сумма по отфильтрованным: " + Формат(Итог, "ЧГ=15.2"));
КонецПроцедуры
```
- Сумма только по выделенным строкам:
```1c
Процедура ПосчитатьСуммуВыделенных(Команда)
    Итог = 0;
    Для каждого Стр Из ЭлементыФормы.Список.ТекущиеДанныеВыделенныхСтрок Цикл
        Итог = Итог + Стр.СуммаДолга;
    КонецЦикла;
    Сообщить("Сумма по выделенным: " + Формат(Итог, "ЧГ=15.2"));
КонецПроцедуры
```
  Подключите этот обработчик к отдельной кнопке или используйте её вместо общей команды.

## 🛟 Подсказки и проверка
- Если нет команды в интерфейсе, проверьте включение «ОткрытьСписок» и размещение в командах раздела.
- В базовой версии недоступны внешние обработки, но новые справочники в конфигураторе создавать можно.
- Делайте резервную копию перед обновлением конфигурации.
