# 🗂️ Инструкция: справочник «Должники» через Конфигуратор 1С:УТ 3.2 (базовая)

## 🧭 Что сделаем
Создадим новый объект «Справочник.Должники» в конфигураторе УТ 3.2, добавим нужные реквизиты и формы, обновим базу и выведем в интерфейс.

## ✅ Предпосылки
- Базовая лицензия УТ 3.2, доступ к конфигуратору.
- Перед изменениями — сделайте резервную копию информационной базы.

## ⚙️ Шаг 1. Открыть конфигурацию
1. Закройте пользовательский сеанс, откройте файл ИБ в Конфигураторе.
2. Меню «Конфигурация» → «Открыть конфигурацию».

## 🧩 Шаг 2. Создать справочник
1. В дереве конфигурации: «Справочники» → правая кнопка → «Добавить».
2. Имя объекта: `Должники`. Синоним: «Должники».
3. Выставьте «Использование стандартных команд» — «Обычное».

## 📐 Шаг 3. Реквизиты
В свойствах справочника → «Реквизиты» → «Добавить»:
- `Контрагент` — Тип: Строка (например, 150; если нужны ИНН/КПП — можно расширить).
- `СуммаДолга` — Тип: Число(15,2).
- `ДатаВзятияДолга` — Тип: Дата (когда взяли долг).
- `ДатаВозвратаДолга` — Тип: Дата (когда долг отдан).
- `Комментарий` — Тип: Строка(200) (по желанию).

## 🪟 Шаг 4. Формы
1. В объекте «Должники» → «Формы» → «Форма списка» → «Открыть» → «Команда: Автосоздание».
2. В таблицу списка добавьте поля: Контрагент, СуммаДолга, ДатаВзятияДолга, ДатаВозвратаДолга, Комментарий.
3. «Форма элемента» → «Открыть» → «Автосоздание» → расположите поля в удобном порядке.

## 🔗 Шаг 5. Команды и размещение
- В свойствах справочника включите «Стандартные команды» → «ОткрытьСписок» (чтобы справочник появился в разделе).
- Если хотите кнопку в разделе «НСИ и администрирование»: «Команды» → «Добавить» → «Открыть список» и поместить в подходящую группу команд.
- Для быстрого доступа в пользовательском режиме можно включить «Отображать в панели функций» (если используется).

## 🚀 Шаг 6. Обновить конфигурацию базы
1. «Конфигурация» → «Проверить» (убедиться, что нет ошибок).
2. «Конфигурация» → «Обновить конфигурацию базы данных» → согласиться на перезапуск.
3. Запустить базу в пользовательском режиме.

## ✍️ Шаг 7. Ввод данных
1. Откройте раздел, где появилась команда «Должники» (или через «Все функции» → Справочники → Должники).
2. «Создать» → заполните Контрагента, Сумму долга, Дату обещанного платежа, Комментарий → Записать и закрыть.

## 🔍 Работа со списком
- Фильтруйте по ДатаОбещанногоПлатежа, чтобы видеть просрочки.
- Сортируйте по СуммаДолга для приоритизации.
- Быстрый поиск по Контрагенту.

## 🔑 Доступ для пользователя «Кассир»
1. В конфигураторе откройте «Роли» → выберите существующую роль кассира (или создайте новую, например `Кассир_Должники`).
2. В дереве прав разверните «Справочники» → «Должники»:
   - Поставьте флажки «Чтение», «Запись», «Использование» (если нужны только просмотр/создание; удаление — по желанию).
3. Обновите конфигурацию базы данных.
4. В пользовательском режиме: «НСИ и администрирование» → «Настройка пользователей и прав» → откройте пользователя «Кассир» → добавьте ему роль, в которой включены права на «Должники» → Записать.
5. Перезапустите сеанс кассира и проверьте, что справочник виден и доступен для ввода.

## ➕ Как посчитать сумму по выбранному сегменту (пример)
Если у вас в справочнике есть поле `Сегмент` (строка или перечисление), можно добавить кнопку на форме списка:
1. В конфигураторе в справочнике добавьте реквизит `Сегмент` (если его еще нет).
2. В форме списка добавьте реквизит формы `ОтборСегмент` (тип тот же, что у `Сегмент`) и поле ввода над списком.
3. Добавьте команду формы «ПосчитатьСумму» и кнопку на панели формы.
4. В модуле формы списка вставьте обработчик (адаптируйте имена элементов):
```1c
Процедура ПосчитатьСумму(Команда)
    Итог = 0;
    Для каждого Стр Из ЭлементыФормы.Список.ТекущиеДанные Цикл
        Если ПустаяСтрока(ОтборСегмент) Или Стр.Сегмент = ОтборСегмент Тогда
            Итог = Итог + Стр.СуммаДолга;
        КонецЕсли;
    КонецЦикла;
    Сообщить("Сумма по сегменту: " + Формат(Итог, "ЧГ=15.2"));
КонецПроцедуры
```
5. В свойствах команды укажите обработчик `ПосчитатьСумму`. Обновите конфигурацию, откройте список «Должники», выберите сегмент и нажмите кнопку.

### Если поля `Сегмент` нет
- Можно считать сумму по текущему отбору списка: используйте стандартные отборы формы (например, по датам), затем кнопку «Посчитать» складывает видимые строки.
- Пример обработчика без сегмента:
```1c
Процедура ПосчитатьСумму(Команда)
    Итог = 0;
    Для каждого Стр Из ЭлементыФормы.Список.ТекущиеДанные Цикл
        Итог = Итог + Стр.СуммаДолга;
    КонецЦикла;
    Сообщить("Сумма по отфильтрованным записям: " + Формат(Итог, "ЧГ=15.2"));
КонецПроцедуры
```
- Если нужна сумма только по выделенным строкам, замените `ЭлементыФормы.Список.ТекущиеДанные` на `ЭлементыФормы.Список.ТекущиеДанныеВыделенныхСтрок`.

## 🛟 Подсказки и проверка
- Если нет команды в интерфейсе, проверьте включение стандартной команды «ОткрытьСписок» и размещение в командах раздела.
- В базовой версии нельзя подключать внешние обработки, но создание справочников в конфигураторе доступно.
- Делайте резервную копию перед обновлением конфигурации.
